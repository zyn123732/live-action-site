# Live Action Studio

Static-exported Next.js site for a cinematic `live action` brand showcase.

## Run locally

```bash
npm install
npm run dev
```

## Build for Cloudflare Pages

```bash
npm run build
```

Upload or connect the repo in Cloudflare Pages with:

- Build command: `npm run build`
- Output directory: `out`
